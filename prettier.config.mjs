export default {
  semi: false, // Без точек с запятой в конце строк
  singleQuote: true, // Одинарные кавычки вместо двойных
  trailingComma: 'es5', // Запятая в конце объектов/массивов (ES5-совместимо)
  printWidth: 100, // Максимальная длина строки
  tabWidth: 2, // Размер таба в пробелах
  useTabs: false, // Пробелы вместо табов
  bracketSpacing: true, // Пробелы внутри { фигурных скобок }
  arrowParens: 'always', // Всегда ставить скобки у стрелочных функций
  endOfLine: 'lf', // Перенос строк в стиле LF (важно для Git)
}
